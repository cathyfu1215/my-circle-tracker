// Import polyfills needed for Firebase
import 'react-native-get-random-values';

// Register the main component
import { registerRootComponent } from 'expo';
import App from './App';

// Register the root component
registerRootComponent(App); 